---
title: Button
description: Button — six variants, three sizes, link or native button.
sidebar:
  order: 15
---

import Preview from '../../../../components/kit/Preview.astro';
import { button } from '../../../../lib/ui';

`button()` builds a button: `variant` switches between Solid / Subtle /
Surface / Outline / Ghost / Plain, `size` between sm / md / lg. Pass `href`
to render an `<a>`, otherwise a native `<button type="button">`.

## Variants

<Preview
  label="All variants"
  html={[
    button({ label: 'Solid', variant: 'solid' }),
    button({ label: 'Subtle', variant: 'subtle' }),
    button({ label: 'Surface', variant: 'surface' }),
    button({ label: 'Outline', variant: 'outline' }),
    button({ label: 'Ghost', variant: 'ghost' }),
    button({ label: 'Plain', variant: 'plain' }),
  ].join(' ')}
/>

```ts
button({ label: 'Solid', variant: 'solid' });
button({ label: 'Subtle', variant: 'subtle' });
button({ label: 'Surface', variant: 'surface' });
button({ label: 'Outline', variant: 'outline' });
button({ label: 'Ghost', variant: 'ghost' });
button({ label: 'Plain', variant: 'plain' });
```

## Sizes

<Preview
  label="sm / md / lg"
  html={[
    button({ label: 'Small', size: 'sm', variant: 'outline' }),
    button({ label: 'Medium', size: 'md', variant: 'outline' }),
    button({ label: 'Large', size: 'lg', variant: 'outline' }),
  ].join(' ')}
/>

```ts
button({ label: 'Small', size: 'sm', variant: 'outline' });
button({ label: 'Medium', size: 'md', variant: 'outline' });
button({ label: 'Large', size: 'lg', variant: 'outline' });
```

## Icon

Icons render before the label; any inline SVG string works.

<Preview
  label="Icon + solid"
  html={button({
    label: 'Call us',
    variant: 'solid',
    icon: '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/></svg>',
  })}
/>

```ts
button({
  label: 'Call us',
  variant: 'solid',
  icon: '<svg …>…</svg>', // any inline SVG string
});
```

## As a link

With `href` the button renders as an `<a>`; `external` opens a new tab.

<Preview
  label="As link"
  html={button({
    label: 'Get started',
    href: '/guides/example/',
    variant: 'solid',
  })}
/>

```ts
button({ label: 'Get started', href: '/guides/example/', variant: 'solid' });
```

## Colors

`color` picks from the kit's semantic tokens (primary / info / success /
warning / error / idea). Solid renders the color with white contrast text.

<Preview
  label="Solid colorPalette"
  html={[
    button({ label: 'Info', variant: 'solid', color: 'info' }),
    button({ label: 'Success', variant: 'solid', color: 'success' }),
    button({ label: 'Warning', variant: 'solid', color: 'warning' }),
    button({ label: 'Error', variant: 'solid', color: 'error' }),
    button({ label: 'Idea', variant: 'solid', color: 'idea' }),
  ].join(' ')}
/>

```ts
button({ label: 'Info', variant: 'solid', color: 'info' });
button({ label: 'Success', variant: 'solid', color: 'success' });
button({ label: 'Warning', variant: 'solid', color: 'warning' });
button({ label: 'Error', variant: 'solid', color: 'error' });
button({ label: 'Idea', variant: 'solid', color: 'idea' });
```

Outline keeps the same palette:

<Preview
  label="Outline colorPalette"
  html={[
    button({ label: 'Info', variant: 'outline', color: 'info' }),
    button({ label: 'Success', variant: 'outline', color: 'success' }),
    button({ label: 'Warning', variant: 'outline', color: 'warning' }),
    button({ label: 'Error', variant: 'outline', color: 'error' }),
    button({ label: 'Idea', variant: 'outline', color: 'idea' }),
  ].join(' ')}
/>

```ts
button({ label: 'Info', variant: 'outline', color: 'info' });
```

## Props

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `label` | `string` | — | Button text (required). |
| `href` | `string` | — | Target URL — renders an `<a>` instead of `<button>`. |
| `variant` | `'solid' \| 'subtle' \| 'surface' \| 'outline' \| 'ghost' \| 'plain'` | `'solid'` | Visual style. |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Height and padding (2rem / 2.5rem / 3rem). |
| `color` | `'primary' \| 'info' \| 'success' \| 'warning' \| 'error' \| 'idea'` | `'primary'` | Color palette from the kit's semantic tokens. |
| `icon` | `string` | `''` | Raw SVG string before the label. |
| `external` | `boolean` | `false` | With `href`: opens the link in a new tab. |
| `attrs` | `Record<string, string>` | — | Extra HTML attributes (e.g. `data-cpd-drawer-trigger`). |

## Notes

- Hover darkens in light mode and lightens in dark mode (`color-mix`);
  focus shows a 2px ring (`--cpd-ring`); disabled renders at 50% opacity.
- Radius `0.375rem`; weight 600; `line-height: 1`.
